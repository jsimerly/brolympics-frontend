# Test catalog

Generated by `npm run test:catalog` -- regenerate after adding tests,
don't edit by hand. Each line is the behavior the test pins, from its
describe/it names (write them as sentences).

Run the suite: `npm test` (check the EXIT CODE -- never pipe to tail).

## src/api/client/events.test.js (3 tests)

- **defaultStagesFor**
  - h2h starts as RR-4 into a seeded top-4 bracket with a 3rd place game
  - ind and team are one open-play outing; ffa deals heats
  - unknown formats fall back to open play instead of crashing the wizard

## src/api/client/invites.test.js (2 tests)

- **invite link builders**
  - build each kind from the configured frontend origin
  - never carry a hardcoded production domain

## src/components/auth/afterAuthPath.test.js (5 tests)

- **afterAuthPath**
  - falls back to home when there is no stashed origin
  - passes a string `from` through verbatim
  - joins pathname and search from a location-object `from`
  - defaults a missing pathname to home but keeps the search
- **isInviteBound**
  - is true only for invite destinations

## src/components/auth/login/LoginWithEmail.test.jsx (3 tests)

- **LoginWithEmail**
  - lands on the stashed invite path after signing in
  - lands home when nothing sent us here
  - surfaces a failed login instead of navigating

## src/components/brolympics/events/eventDisplay.test.js (9 tests)

- **groupLog**
  - labels playoff rounds with the max round as Finals
  - labels numbered non-knockout rounds and bare games
  - returns nothing for an empty log
- **stageSentence**
  - tells the truth about each structure
- **SCORE_FORMAT_LABEL**
  - maps stored decimal_places 0 to Whole numbers -- the falsy-zero trap
- **hasRules**
  - sees through the Quill empty-editor ghost
- **placeLabel**
  - names known places and never says Nullth
- **groupBracketNodes**
  - splits championship tree from placement games, best place first
  - survives a dangling winner_to pointer

## src/components/Util/dates.test.js (3 tests)

- **daysUntil**
  - counts whole days up (ceil) to the start date
  - is 0-or-negative once the date arrives, null when absent or junk
- **formatDateRange**
  - renders both ends when known, one end otherwise, null when neither

## src/components/Util/dateUtils.test.js (2 tests)

- **convertDateFromDjango**
  - formats a local ISO string for a datetime-local input
  - zero-pads every component

## src/components/Util/format.test.js (5 tests)

- **trimFloat**
  - cleans float-sum noise to what a human would write
  - leaves integers and non-numbers alone
- **isScoreInput**
  - accepts free-typed decimals, including intermediate states
  - rejects non-numeric junk
- **ordinal**
  - handles the English edge cases

## src/components/Util/input_validation.test.js (9 tests)

- **isValidDate_MMDDYYYY**
  - accepts real calendar dates in MM-DD-YYYY
  - rejects impossible dates and wrong formats
- **isOver18**
  - draws the line at exactly 18 years
- **isValidEmail**
  - accepts normal addresses and rejects junk
- **convertDateFormat_MMDDYYY_to_YYYYMMDD**
  - round-trips the digits without timezone drift
- **AccountValidator**
  - cleanPhoneNumber produces the +1 E.164 form Firebase wants
  - validatePassword demands 8+ chars with an uppercase and a special
  - validatePhoneNumber wants 555-555-5555 and flags anything else
  - verifyPasswordsMatch catches mismatches and resetErrors clears

## src/components/Util/stageBuilder.test.js (12 tests)

- **buildStages**
  - default h2h: semi round robin into a seeded top-4 with a 3rd place game
  - full RR and swiss group play
  - no group play still forces a bracket -- an event needs games
  - custom run-offs: take 8 played through 4th skips the 5th and 7th games
  - custom on a bracket under 6 has nothing to skip
  - run-offs off and 'every place' both build what they say
  - whole-field bracket when take is blank
  - ffa builds heats (sized only when >= 2); ind/team build open play
- **formFromStages**
  - reads a stored RR+KO event back into form choices
  - distinguishes every-place from custom and third from off
- **round trip**
  - formFromStages(buildStages(form)) preserves every structure choice
  - a reopened saved event saves back byte-identical stages

---

53 tests cataloged.
